# Nim configuration for Burrito project

# Output directory for compiled binaries
--outdir:"build/bin"

# Nim cache directory (optional, keeps cache organized)
--nimcache:"build/nimcache"

# Common compiler flags
--hints:off
--warnings:on

# Threading support (required for some QuickJS features)
--threads:on

# Optimization for release builds
# Users can override with: nim c -d:release
--opt:none

# Path configuration is handled in burrito.nim via {.passC.}